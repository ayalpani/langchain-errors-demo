{
  "name": "mono-mono",
  "version": "1.0.0",
  "description": "",
  "main": "build/index.js",
  "author": "",
  "license": "ISC",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node build/index.js",
    "build": "esbuild --bundle --minify --platform=node --target=node18 --outdir=build/ src/*.ts*"
  },
  "devDependencies": {
    "esbuild": "^0.17.11"
  },
  "dependencies": {
    "langchain": "^0.0.32"
  }
}
